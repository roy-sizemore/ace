const router = require('express').Router();
const { Competency } = require('../../models');
const withAuth = require('../../utils/auth');

// Same roadblock as trainingRoutes.js

module.exports = router;
