
const PDFDocument = require("pdfkit"), fs = require("fs"), doc = new PDFDocument({ font: "Helvetica", layout: "landscape" }); doc.pipe(fs.createWriteStream("certificate.pdf")); const user = { name: "Bob" }, training_name = "Java"; doc.fontSize(35).text("Certificate of Completion", 100, 80, { align: "center" }).fillColor("blue").moveDown(3), doc.text(`Congratulations ${user.name}!`, { align: "center" }).moveDown(3).text(`Successful completion of the ${training_name} training!`, { size: 25, align: "center" }), doc.end();
